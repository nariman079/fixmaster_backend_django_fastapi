name: Production CI/CD Pipeline 

on:
  push:
    branches: 
      - master


jobs:
  test:
    runs-on: ubuntu-latest
    steps: 
      - name: Checkout code 
        uses: actions/checkout@v4
      
      # 🔍 1. Статический анализ кода (SAST)
      - name: SAST 
        uses: docker://ghcr.io/py-foundations/bandit-action:latest
        with: 
          args: -r .
          
      # 🔍 2. Проверка уязвимостей в зависимостях (SCA)
      # 🐳 3. Сборка образа и проверка Docker-файла
      # 🔍 4. Сканирование образа на уязвимости (в контейнере)
      # 🧪 5. Запуск тестов (если есть)

      # 🚀 6. (Опционально) Деплой в dev-среду
