name: Production CI/CD Pipeline 

on:
  push:
    branches: [ main ]


jobs:
  test:
    runs-on: ubunut-latest
    steps: 
      - name: Checkout code 
        uses: actions/checkout@v4

      - name: Test run
        run: echo "test"